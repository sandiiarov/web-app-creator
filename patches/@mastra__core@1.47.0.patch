diff --git a/dist/chunk-F5V6RIYO.js b/dist/chunk-F5V6RIYO.js
index ce8ef1a91e0dabe935929921b0f47ef7900c1147..5fcd197354888a9adc9b2973615af49c8dd6b578 100644
--- a/dist/chunk-F5V6RIYO.js
+++ b/dist/chunk-F5V6RIYO.js
@@ -960,7 +960,7 @@ var OpenAIChatLanguageModel = class {
                         delta: toolCall2.function.arguments
                       });
                     }
-                    if (isParsableJson(toolCall2.function.arguments)) {
+                    if (false) {
                       controller.enqueue({
                         type: "tool-input-end",
                         id: toolCall2.id
@@ -988,7 +988,7 @@ var OpenAIChatLanguageModel = class {
                   id: toolCall.id,
                   delta: (_u = toolCallDelta.function.arguments) != null ? _u : ""
                 });
-                if (((_v = toolCall.function) == null ? void 0 : _v.name) != null && ((_w = toolCall.function) == null ? void 0 : _w.arguments) != null && isParsableJson(toolCall.function.arguments)) {
+                if (((_v = toolCall.function) == null ? void 0 : _v.name) != null && ((_w = toolCall.function) == null ? void 0 : _w.arguments) != null && false) {
                   controller.enqueue({
                     type: "tool-input-end",
                     id: toolCall.id
@@ -1019,6 +1019,13 @@ var OpenAIChatLanguageModel = class {
             if (isActiveText) {
               controller.enqueue({ type: "text-end", id: "0" });
             }
+            for (const _tc of toolCalls) {
+              if (_tc && !_tc.hasFinished) {
+                controller.enqueue({ type: "tool-input-end", id: _tc.id });
+                controller.enqueue({ type: "tool-call", toolCallId: _tc.id != null ? _tc.id : generateId(), toolName: _tc.function.name, input: _tc.function.arguments });
+                _tc.hasFinished = true;
+              }
+            }
             controller.enqueue({
               type: "finish",
               finishReason,
diff --git a/dist/chunk-GHDHOLZS.js b/dist/chunk-GHDHOLZS.js
index 33e6d4b73b127bf493113919a8a0017b532a0487..85c0dbc388f0dc6eda4796e0ca7522fff2e4dbac 100644
--- a/dist/chunk-GHDHOLZS.js
+++ b/dist/chunk-GHDHOLZS.js
@@ -8775,7 +8775,20 @@ function convertToOpenRouterChatMessages(prompt) {
   return messages;
 }
 function getToolResultContent(input) {
-  return input.output.type === "text" ? input.output.value : JSON.stringify(input.output.value);
+  // PATCHED: serialize multimodal `content` tool results as chat-completions
+  // content parts (text + image_url) instead of JSON-stringifying them, so
+  // screenshots/images returned by tools reach vision-capable models.
+  if (input.output.type === "text") return input.output.value;
+  if (input.output.type === "content" && Array.isArray(input.output.value)) {
+    return input.output.value.map((item) => {
+      if (item.type === "text") return { type: "text", text: item.text };
+      if (item.type === "media" && typeof item.mediaType === "string" && item.mediaType.startsWith("image/")) {
+        return { type: "image_url", image_url: { url: `data:${item.mediaType};base64,${item.data}` } };
+      }
+      return { type: "text", text: JSON.stringify(item) };
+    });
+  }
+  return JSON.stringify(input.output.value);
 }
 z.union([
   z.literal("auto"),
@@ -9559,35 +9572,6 @@ var OpenRouterChatLanguageModel = class {
                       message: `Tool call at index ${index} is missing after creation.`
                     });
                   }
-                  if (((_g = toolCall2.function) == null ? void 0 : _g.name) != null && ((_h = toolCall2.function) == null ? void 0 : _h.arguments) != null && isParsableJson3(toolCall2.function.arguments)) {
-                    toolCall2.inputStarted = true;
-                    controller.enqueue({
-                      type: "tool-input-start",
-                      id: toolCall2.id,
-                      toolName: toolCall2.function.name
-                    });
-                    controller.enqueue({
-                      type: "tool-input-delta",
-                      id: toolCall2.id,
-                      delta: toolCall2.function.arguments
-                    });
-                    controller.enqueue({
-                      type: "tool-input-end",
-                      id: toolCall2.id
-                    });
-                    controller.enqueue({
-                      type: "tool-call",
-                      toolCallId: toolCall2.id,
-                      toolName: toolCall2.function.name,
-                      input: toolCall2.function.arguments,
-                      providerMetadata: {
-                        openrouter: {
-                          reasoning_details: accumulatedReasoningDetails
-                        }
-                      }
-                    });
-                    toolCall2.sent = true;
-                  }
                   continue;
                 }
                 const toolCall = toolCalls[index];
@@ -9601,35 +9585,14 @@ var OpenRouterChatLanguageModel = class {
                     message: `Tool call at index ${index} is missing during merge.`
                   });
                 }
-                if (!toolCall.inputStarted) {
-                  toolCall.inputStarted = true;
-                  controller.enqueue({
-                    type: "tool-input-start",
-                    id: toolCall.id,
-                    toolName: toolCall.function.name
-                  });
-                }
-                if (((_i = toolCallDelta.function) == null ? void 0 : _i.arguments) != null) {
-                  toolCall.function.arguments += (_k = (_j = toolCallDelta.function) == null ? void 0 : _j.arguments) != null ? _k : "";
-                }
-                controller.enqueue({
-                  type: "tool-input-delta",
-                  id: toolCall.id,
-                  delta: (_l = toolCallDelta.function.arguments) != null ? _l : ""
-                });
-                if (((_m = toolCall.function) == null ? void 0 : _m.name) != null && ((_n = toolCall.function) == null ? void 0 : _n.arguments) != null && isParsableJson3(toolCall.function.arguments)) {
-                  controller.enqueue({
-                    type: "tool-call",
-                    toolCallId: (_o = toolCall.id) != null ? _o : generateId3(),
-                    toolName: toolCall.function.name,
-                    input: toolCall.function.arguments,
-                    providerMetadata: {
-                      openrouter: {
-                        reasoning_details: accumulatedReasoningDetails
-                      }
-                    }
-                  });
-                  toolCall.sent = true;
+                const argumentDelta = (_i = toolCallDelta.function.arguments) != null ? _i : "";
+                if (argumentDelta !== "") {
+                  const currentArguments = toolCall.function.arguments;
+                  if (currentArguments.trim() === "{}" && argumentDelta.trim().startsWith("{") || argumentDelta.startsWith(currentArguments)) {
+                    toolCall.function.arguments = argumentDelta;
+                  } else {
+                    toolCall.function.arguments += argumentDelta;
+                  }
                 }
               }
             }
@@ -9644,31 +9607,49 @@ var OpenRouterChatLanguageModel = class {
             }
           },
           flush(controller) {
-            var _a16;
             const hasToolCalls = toolCalls.length > 0;
-            const hasEncryptedReasoning = accumulatedReasoningDetails.some(
-              (d) => d.type === "reasoning.encrypted" && d.data
-            );
-            if (hasToolCalls && hasEncryptedReasoning && finishReason === "stop") {
+            if (hasToolCalls && finishReason === "stop") {
               finishReason = "tool-calls";
             }
-            if (finishReason === "tool-calls") {
-              for (const toolCall of toolCalls) {
-                if (toolCall && !toolCall.sent) {
-                  controller.enqueue({
-                    type: "tool-call",
-                    toolCallId: (_a16 = toolCall.id) != null ? _a16 : generateId3(),
-                    toolName: toolCall.function.name,
-                    // Coerce invalid arguments to an empty JSON object
-                    input: isParsableJson3(toolCall.function.arguments) ? toolCall.function.arguments : "{}",
-                    providerMetadata: {
-                      openrouter: {
-                        reasoning_details: accumulatedReasoningDetails
-                      }
-                    }
+            for (const toolCall of toolCalls) {
+              if (toolCall && !toolCall.sent) {
+                const input = toolCall.function.arguments;
+                if (!isParsableJson3(input)) {
+                  throw new InvalidResponseDataError3({
+                    data: {
+                      argumentLength: input.length,
+                      toolCallId: toolCall.id,
+                      toolName: toolCall.function.name
+                    },
+                    message: "OpenRouter stream ended with incomplete tool-call arguments."
                   });
-                  toolCall.sent = true;
                 }
+                controller.enqueue({
+                  type: "tool-input-start",
+                  id: toolCall.id,
+                  toolName: toolCall.function.name
+                });
+                controller.enqueue({
+                  type: "tool-input-delta",
+                  id: toolCall.id,
+                  delta: input
+                });
+                controller.enqueue({
+                  type: "tool-input-end",
+                  id: toolCall.id
+                });
+                controller.enqueue({
+                  type: "tool-call",
+                  toolCallId: toolCall.id != null ? toolCall.id : generateId3(),
+                  toolName: toolCall.function.name,
+                  input,
+                  providerMetadata: {
+                    openrouter: {
+                      reasoning_details: accumulatedReasoningDetails
+                    }
+                  }
+                });
+                toolCall.sent = true;
               }
             }
             if (reasoningStarted) {
diff --git a/dist/chunk-YYUDVZJC.cjs b/dist/chunk-YYUDVZJC.cjs
index ed85bcd8b8dea06ba08b26d3e070cfb5f369fd42..b77d63da1aec9b99fe9f801921ef14b86ca8648f 100644
--- a/dist/chunk-YYUDVZJC.cjs
+++ b/dist/chunk-YYUDVZJC.cjs
@@ -8777,7 +8777,20 @@ function convertToOpenRouterChatMessages(prompt) {
   return messages;
 }
 function getToolResultContent(input) {
-  return input.output.type === "text" ? input.output.value : JSON.stringify(input.output.value);
+  // PATCHED: serialize multimodal `content` tool results as chat-completions
+  // content parts (text + image_url) instead of JSON-stringifying them, so
+  // screenshots/images returned by tools reach vision-capable models.
+  if (input.output.type === "text") return input.output.value;
+  if (input.output.type === "content" && Array.isArray(input.output.value)) {
+    return input.output.value.map((item) => {
+      if (item.type === "text") return { type: "text", text: item.text };
+      if (item.type === "media" && typeof item.mediaType === "string" && item.mediaType.startsWith("image/")) {
+        return { type: "image_url", image_url: { url: `data:${item.mediaType};base64,${item.data}` } };
+      }
+      return { type: "text", text: JSON.stringify(item) };
+    });
+  }
+  return JSON.stringify(input.output.value);
 }
 v4.z.union([
   v4.z.literal("auto"),
@@ -9561,35 +9574,6 @@ var OpenRouterChatLanguageModel = class {
                       message: `Tool call at index ${index} is missing after creation.`
                     });
                   }
-                  if (((_g = toolCall2.function) == null ? void 0 : _g.name) != null && ((_h = toolCall2.function) == null ? void 0 : _h.arguments) != null && isParsableJson3(toolCall2.function.arguments)) {
-                    toolCall2.inputStarted = true;
-                    controller.enqueue({
-                      type: "tool-input-start",
-                      id: toolCall2.id,
-                      toolName: toolCall2.function.name
-                    });
-                    controller.enqueue({
-                      type: "tool-input-delta",
-                      id: toolCall2.id,
-                      delta: toolCall2.function.arguments
-                    });
-                    controller.enqueue({
-                      type: "tool-input-end",
-                      id: toolCall2.id
-                    });
-                    controller.enqueue({
-                      type: "tool-call",
-                      toolCallId: toolCall2.id,
-                      toolName: toolCall2.function.name,
-                      input: toolCall2.function.arguments,
-                      providerMetadata: {
-                        openrouter: {
-                          reasoning_details: accumulatedReasoningDetails
-                        }
-                      }
-                    });
-                    toolCall2.sent = true;
-                  }
                   continue;
                 }
                 const toolCall = toolCalls[index];
@@ -9603,35 +9587,14 @@ var OpenRouterChatLanguageModel = class {
                     message: `Tool call at index ${index} is missing during merge.`
                   });
                 }
-                if (!toolCall.inputStarted) {
-                  toolCall.inputStarted = true;
-                  controller.enqueue({
-                    type: "tool-input-start",
-                    id: toolCall.id,
-                    toolName: toolCall.function.name
-                  });
-                }
-                if (((_i = toolCallDelta.function) == null ? void 0 : _i.arguments) != null) {
-                  toolCall.function.arguments += (_k = (_j = toolCallDelta.function) == null ? void 0 : _j.arguments) != null ? _k : "";
-                }
-                controller.enqueue({
-                  type: "tool-input-delta",
-                  id: toolCall.id,
-                  delta: (_l = toolCallDelta.function.arguments) != null ? _l : ""
-                });
-                if (((_m = toolCall.function) == null ? void 0 : _m.name) != null && ((_n = toolCall.function) == null ? void 0 : _n.arguments) != null && isParsableJson3(toolCall.function.arguments)) {
-                  controller.enqueue({
-                    type: "tool-call",
-                    toolCallId: (_o = toolCall.id) != null ? _o : generateId3(),
-                    toolName: toolCall.function.name,
-                    input: toolCall.function.arguments,
-                    providerMetadata: {
-                      openrouter: {
-                        reasoning_details: accumulatedReasoningDetails
-                      }
-                    }
-                  });
-                  toolCall.sent = true;
+                const argumentDelta = (_i = toolCallDelta.function.arguments) != null ? _i : "";
+                if (argumentDelta !== "") {
+                  const currentArguments = toolCall.function.arguments;
+                  if (currentArguments.trim() === "{}" && argumentDelta.trim().startsWith("{") || argumentDelta.startsWith(currentArguments)) {
+                    toolCall.function.arguments = argumentDelta;
+                  } else {
+                    toolCall.function.arguments += argumentDelta;
+                  }
                 }
               }
             }
@@ -9646,31 +9609,49 @@ var OpenRouterChatLanguageModel = class {
             }
           },
           flush(controller) {
-            var _a16;
             const hasToolCalls = toolCalls.length > 0;
-            const hasEncryptedReasoning = accumulatedReasoningDetails.some(
-              (d) => d.type === "reasoning.encrypted" && d.data
-            );
-            if (hasToolCalls && hasEncryptedReasoning && finishReason === "stop") {
+            if (hasToolCalls && finishReason === "stop") {
               finishReason = "tool-calls";
             }
-            if (finishReason === "tool-calls") {
-              for (const toolCall of toolCalls) {
-                if (toolCall && !toolCall.sent) {
-                  controller.enqueue({
-                    type: "tool-call",
-                    toolCallId: (_a16 = toolCall.id) != null ? _a16 : generateId3(),
-                    toolName: toolCall.function.name,
-                    // Coerce invalid arguments to an empty JSON object
-                    input: isParsableJson3(toolCall.function.arguments) ? toolCall.function.arguments : "{}",
-                    providerMetadata: {
-                      openrouter: {
-                        reasoning_details: accumulatedReasoningDetails
-                      }
-                    }
+            for (const toolCall of toolCalls) {
+              if (toolCall && !toolCall.sent) {
+                const input = toolCall.function.arguments;
+                if (!isParsableJson3(input)) {
+                  throw new InvalidResponseDataError3({
+                    data: {
+                      argumentLength: input.length,
+                      toolCallId: toolCall.id,
+                      toolName: toolCall.function.name
+                    },
+                    message: "OpenRouter stream ended with incomplete tool-call arguments."
                   });
-                  toolCall.sent = true;
                 }
+                controller.enqueue({
+                  type: "tool-input-start",
+                  id: toolCall.id,
+                  toolName: toolCall.function.name
+                });
+                controller.enqueue({
+                  type: "tool-input-delta",
+                  id: toolCall.id,
+                  delta: input
+                });
+                controller.enqueue({
+                  type: "tool-input-end",
+                  id: toolCall.id
+                });
+                controller.enqueue({
+                  type: "tool-call",
+                  toolCallId: toolCall.id != null ? toolCall.id : generateId3(),
+                  toolName: toolCall.function.name,
+                  input,
+                  providerMetadata: {
+                    openrouter: {
+                      reasoning_details: accumulatedReasoningDetails
+                    }
+                  }
+                });
+                toolCall.sent = true;
               }
             }
             if (reasoningStarted) {
