# Implementation Plans

Generated by the improve skill on 2026-07-14 against commit `e0cae5de`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

These two plans are independent — either may run first, and they touch
disjoint files (`pnpm-workspace.yaml`/`pnpm-lock.yaml` vs. server source), so
they can even be executed in parallel worktrees without conflict.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Bump vite catalog to ^8.0.16 (clear HIGH advisory) | P2 | S | — | DONE |
| 002  | Extract route.ts tool-display helpers into lib/tool-display.ts | P3 | S–M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Execution log

Executed directly by the advisor at the user's explicit request (overriding the
advisor/executor separation); acting as own reviewer. Verified against the
full CI chain (`format:check · lint · typecheck · test · build`).

### 001 — DONE
- Catalog `vite: ^8.0.14` → `^8.0.16`; `pnpm install` resolved **8.1.4**
  (latest in range — a minor bump, within the plan's escape hatch since the
  resolved version carries no advisory).
- Gates: `pnpm install --frozen-lockfile` exit 0; `pnpm audit --prod` no longer
  lists vite (9→7 vulns, 2→1 HIGH; remaining HIGH is transitive `hono` via
  `@mastra/core`); `pnpm --filter @workspace/client build` exit 0.
- Scope: only `pnpm-workspace.yaml` + `pnpm-lock.yaml`.

### 002 — DONE
- Created `apps/server/src/mastra/lib/tool-display.ts`; `route.ts` diff is
  exactly +14 lines (one import block) / −326 (pure code motion).
- **Documented deviation from the plan's symbol table:** the table also needed
  `compactLines`, `stringValue`, and `expandScreenshotUrl` moved + exported —
  they're shared with `route.ts` (`buildHistory*`, `startToolCallDisplay`,
  `analyzePromptAttachments`). Typecheck caught the two missed exports; the
  third (`compactLines`) was identified during verbatim transcription.
- Gates: typecheck exit 0; lint exit 0 (`lint:fix` reordered the new file for
  `perfectionist/sort-modules` — touched ONLY `tool-display.ts`, confirmed via
  mtimes that the user's pre-existing WIP files were untouched);
  test exit 0 — **246 passed | 1 skipped, identical to baseline**, coverage
  unchanged; `format:check` exit 0; no moved symbols remain in `route.ts`;
  no import cycle.
- `pnpm run fallow:dead-code` exits 1 on a **pre-existing** item
  (`@workspace/agent-skills` dormant dep, in `package.json` at HEAD, documented
  as retained-for-reversibility) — NOT introduced by this plan; none of the
  moved symbols are flagged. Tracked in "rejected".
- DOX closeout: added a `tool-display.ts` pointer to
  `apps/server/src/mastra/AGENTS.md` lib ownership.

## Dependency notes

- 001 and 002 are independent (disjoint file sets). No ordering required.
- Recommended sequence if doing both sequentially: **001 first** (smallest,
  clears a CI-relevant advisory), then **002** (the larger refactor benefits
  from a clean green baseline).

## Context carried forward

- `route.ts` had **uncommitted WIP** at planning time. Plan 002's excerpts
  reflect the live working tree, not commit `e0cae5de`; its drift check + STOP
  condition handle the mismatch. Plan 001's files were clean at HEAD.
- Verification baseline (planning time, server workspace): `typecheck` green,
  `test` green (84% lines, 30+ route integration tests), CI runs
  `format:check · lint · typecheck · fallow:dead-code · test · build` with
  `pnpm install --frozen-lockfile`.

## Findings considered and rejected

(So nobody re-audits them next run.)

- **Dead `skill`/`skill_read`/`skill_search` branches in `route.ts`**
  (`defaultToolAction`, `summarizeArgsForTool`, `summarizeResultForTool`):
  by-design — `apps/server/src/mastra/AGENTS.md` explicitly retains them as
  "harmless dead code" for reversibility. (Note: plan 002 moves these branches
  verbatim into `lib/tool-display.ts`; they remain dead-by-design there.)
- **Transitive `hono` HIGH + `@ai-sdk/provider-utils` LOW advisories**: arrive
  through `@mastra/core@1.47.0`, pinned by the load-bearing
  `patches/@mastra__core@1.47.0.patch`. Not actionable without a Mastra
  upgrade + patch regeneration. Monitor, don't plan.
- **SSRF via scraped-image OCR fetch** (`image-ocr.ts` `fetchAsDataUrl`, no
  internal-IP blocklist): LOW confidence given the documented loopback /
  operator-controlled deployment model. Surfaced to the maintainer; not
  planned pending a threat-model decision (would be an opt-in private-address
  blocklist, not a default-on change).
- **No `OPENROUTER_API_KEY` pre-flight**: low value; tools already return clear
  errors and the stream surfaces the 401 retry path.
- **`getProject` turnCache replays the whole client log on a miss**: only on
  transport-loss reconciliation, not steady-state streaming; immaterial at
  this scale.
- **Local-browser screenshot fallback when Cloudflare unconfigured**
  (direction D1) and **per-project spend budget** (direction D2): presented as
  grounded direction options; not selected this round.
