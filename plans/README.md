# Implementation Plans

Generated by the `improve` skill. Initial audit 2026-07-07 at commit `87e1666b` (plans 001–008, all DONE); second audit 2026-07-07 at commit `4e199c45` (plans 009–011, all DONE); third audit 2026-07-09 at commit `a43ca336`, informed by the 5-project e2e QA campaign (`screenshots/e2e-5run/REPORT.md`), plans 012–015. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, run its drift check first, and update your status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Rewrite the stale README to match the OpenRouter/Mastra server | P1 | S | — | DONE |
| 002  | Serve persisted screenshots via a project route | P1 | S | — | DONE |
| 003  | Cache the replayed client log in getProject | P3 | M | — | DONE |
| 004  | Add a per-run cost cap to the /agent stream | P2 | M | — | DONE |
| 005  | Log scrape-tool OCR calls to vision-messages.json | P3 | M | — | DONE |
| 006  | Extract the shared event→turns reducer (kill drift) | P2 | M | — | DONE — `@workspace/conversation` package; server hydrates via `replayClientEvents`, client live-streams via `applyEventToTurn`. The `retry` divergence is preserved as a documented client-local display part (server skips on reload). Live-verified: reload renders identically to the stream. |
| 007  | Delete the dead `record*` in-memory recording code | P3 | M | — (sequence after 004/005) | DONE |
| 008  | Comprehensive characterization tests for the reducer | P2 | M | 006 | DONE — 14 characterization tests in `@workspace/conversation` (moved from the server), incl. the `retry` divergence pin and an immutability check. |
| 009  | Sync the README edit-engine description with hashline + agent-map | P2 | S | — | DONE |
| 010  | Export the generated landing page as standalone single-file HTML | P2 | M | — | DONE — `GET /api/projects/:id/html` inlines project images as base64 data URLs (portable single-file); client `downloadProjectHtml` + PromptPanel header button (disabled until HTML exists); 3 route tests incl. inlining happy-path. |
| 011  | Exclude the vendored hashline engine from fallow dead-code analysis | P3 | S | — | DONE — `.fallowrc.jsonc` `ignorePatterns` for `lib/hashline/**` + `ignoreDependencies` for diff/lru-cache/xxhash-wasm (hashline's deps); also dropped 4 first-party dead exports surfaced by the exclude (UI_TO_ARTIFACT_SYSTEM_PROMPT, 3 ProjectMessage* re-exports, AgentMapOptions). `fallow:dead-code` exits 0. |
| 012  | Capture `generate_image` cost via a per-image price fallback | P1 | S | — | TODO |
| 013  | Add a timeout + retry to screenshot OCR and surface failures | P2 | M | — | TODO |
| 014  | Add a minimal-change principle for fix/refine requests to the agent instructions | P2 | S | — | TODO |
| 015  | Surface image-generation usage and cost to the user (direction spike) | P3 | M | 012 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency / sequencing notes

- **001, 002 first** — both S, LOW risk, independent. Do them before anything else.
- **006 done, 008 done** — the shared reducer lives in `@workspace/conversation`; 008's tests moved into that package.
- **007 conflicts with 004 and 005** — all three edit the `streamLoop` in `apps/server/src/mastra/route.ts`. Do not run them in parallel; sequence them. (004 may add one `recordTurnError` line that 007 then removes — 007's notes call this out.)
- **002, 003, 004, 005, 006 are mutually independent** and can be parallelized by different executors if the host supports it.
- Suggested batching: {001,002} → {003,004,005} → {007 after 004/005} → {006} → {008}. All resolved.
- **009, 010, 011 are mutually independent** (second audit, `4e199c45`) and touch disjoint files — safe to parallelize. 009 = README docs (S, LOW); 010 = HTML export spike (M, the real product gap); 011 = fallow config exclude for vendored hashline (S, LOW).

## Verification baseline (every plan re-runs its subset)

- `pnpm install`
- `pnpm run typecheck` (all workspaces)
- `pnpm run lint`
- `pnpm run format:check`
- `pnpm run test` (server has a 90% coverage gate; don't let it drop)
- Focused: `pnpm --filter @workspace/server test -- --run [<pattern>]`

## Findings considered and rejected

- **CI (`.github/workflows`)**: DONE — implemented in commit `9499e2fb` (`.github/workflows/ci.yml`: pnpm 11.1.3 + Node 22, frozen-lockfile install, sequential gates format:check → lint → typecheck → fallow:dead-code → test → build, concurrency group with cancel-in-progress). The earlier "operator declined" rejection is superseded.
- **Path traversal on image/screenshot routes** (second audit, `4e199c45`): disproven — `isSafeImageName`/`isSafeScreenshotName` regex + `..`/`/` rejection + UUID-constrained IDs (`apps/server/src/index.ts`, `project-store.ts`). Solid; don't re-audit unless those guards change.
- **CORS `*` default** (`CLIENT_ORIGIN=*`): by-design for a no-auth local dev tool; no credentials/cookies in play. Not a finding.
- **Client-disconnect listener leak in `route.ts`**: disproven — `finally { request.off('close', onClose) }` removes the listener. No leak.
- **Empty `else {}` in `route.ts` outer catch**: trivial dead branch; not plan-worthy.
