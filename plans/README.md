# Implementation Plans

Generated by the improve skill on 2026-08-02 (reconciled with the
2026-07-19 backlog; stale plans refreshed in place). Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 009 | Make the `fallow:dead-code` CI gate green again | P1 | S | — | DONE |
| 008 | Stop leaking `error.message` in 500 responses | P1 | S | — | DONE (executed 2026-08-02) |
| 007 | Derive `baseUrl` from config (Host-header hardening) | P1 | S | — | DONE (executed 2026-08-02) |
| 002 | Surface `chainProjectWrite` log-write failures | P1 | S | — | TODO (refreshed 2026-08-02) |
| 003 | Bound the `turnCache` Map with `LRUCache` | P2 | S | see notes | TODO (refreshed 2026-08-02) |
| 004 | Delete the dead `grep` tool and its search helper | P2 | S | 009 | TODO (refreshed 2026-08-02) |
| 010 | Bound `/api/models?ids=` to stop upstream fetch amplification | P2 | S | — | TODO |
| 013 | End-to-end verification of direct-mode image delivery | P2 | M | — | TODO |
| 011 | Extract `runAgentStream` into composable units | P2 | L | 009 (soft) | TODO |
| 012 | Drive the picker's vision-sync from live OpenRouter modalities | P3 | M | — | TODO |
| 001 | Delete the dead legacy turn writers | — | — | — | DONE (moot — only read-only legacy fallback remains, by design) |
| 005 | Accumulate `warnings` across multi-section edits | — | — | — | DONE (engine.ts accumulates warnings across ranges) |
| 006 | Fix the stale `request('close')` comment in `route.ts` | — | — | — | DONE (superseded by ae221a8d — index.ts listens on response close) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **009 first**: it greens the `fallow:dead-code` CI gate every other plan's
  verification relies on, and it removes the unused `lru-cache` dep.
- **003 after 009**: 009 removes `lru-cache` from `apps/server/package.json`
  (unused today); 003 re-adds the identical line when it puts the dep to
  use. 003's instructions cover both orders.
- **004 after 009**: its fallow-gate verification expects a clean baseline.
- **002 and 003 same file**: both touch `project-store.ts` — run in separate
  worktrees from the same SHA and merge one at a time (conflicts are
  mechanical, different sections).
- **011 last among code plans**: it restructures `route.ts`; smaller
  in-flight plans touching `route.ts` should land first to avoid re-drift.

## Findings considered and rejected

- **`@workspace/agent-skills` unused dep**: not a removal candidate —
  documented-dormant package kept for reversibility (see
  `apps/server/src/mastra/AGENTS.md`); plan 009 suppresses the flag instead.
- **`imagePricingCache` entry-count bound** (model-catalog.ts): with the
  64-id request cap (plan 010) this is a slow leak at worst; not worth a
  plan now.
- **Sharing one OpenRouter catalog cache between `model-catalog.ts` and
  `mastra/lib/model-capabilities.ts`**: both cheap, different TTL needs;
  noted in plan 012 maintenance, not a plan.
- **Moving `analyzePromptAttachments` out of route.ts**: already a cohesive
  unit; noted as follow-up in plan 011, not bundled.
- **`tool-display.ts` dead `grep:`/`skill*` display branches**: harmless,
  behavior-neutral; removing them is cosmetic and not worth a dedicated
  plan (may ride along with plan 004 review if the reviewer insists).
