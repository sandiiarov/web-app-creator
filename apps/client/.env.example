# Landing-page client env sample.
# Copy to `.env` and adjust if the server is not at the default address.
# Real `.env` is gitignored; never commit secrets.

# Base URL of the landing-page agent server. Used as `import.meta.env.VITE_SERVER_URL`.
# VITE_SERVER_URL=http://localhost:3001
